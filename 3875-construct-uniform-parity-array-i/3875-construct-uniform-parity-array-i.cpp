class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        // vector<int>arr;

        // for(int i=0;i<n;i++)
        // {
        //     if(nums[i]&1)
        //     arr.push_back(nums[i]);

        //     if()
        // }

        return true;
    }
};